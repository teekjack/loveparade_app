<!DOCTYPE html>
<html>
  <head>
		<link rel="shortcut icon" href="/favicon.ico" />
    <title>Loveparade Videos</title>
    <%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
		<%= javascript_include_tag :defaults %>
  </head>
  <body>
		<header></header>
		
		<section class="round">
    	<%= yield %>
		</section>
		<%= debug(params) if Rails.env.development? %>

  </body>
</html>

